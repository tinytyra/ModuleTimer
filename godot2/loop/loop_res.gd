extends Resource
class_name Loop
# Custom resource class for loop data .tres files
# Used by: profile_res.gd
# Uses: block_res.gd

@export var has_blocks : Array[Resource]
@export var auto_repeat : bool
